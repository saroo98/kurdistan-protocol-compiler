name: Legacy Phase 16 cloud plan (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Superseded architecture
        shell: bash
        run: |
          echo "phase16-cloud-experiment-disabled"
          echo "KIP-0093 replaced the centralized cloud plan with owner-managed self-hosting."
          exit 1
