# Relay Bridge Fixtures

These fixtures are deterministic safe summaries for the relay bridge model. They contain synthetic bridge session classes, stream mapping classes, lifecycle counts, hashes, and gate evidence only.

They do not contain raw payloads, raw bytes, endpoint data, resolver data, DNS queries, packet captures, keys, nonces, auth tags, proof material, or secrets.
