# Proxy Egress Fixtures

These fixtures are deterministic safe summaries for the local proxy egress model. They contain synthetic descriptor classes, lifecycle states, counts, hashes, and gate evidence only.

They do not contain raw payloads, raw bytes, endpoint data, resolver data, DNS queries, packet captures, keys, nonces, auth tags, proof material, or secrets.
