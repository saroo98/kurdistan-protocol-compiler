# Protocol Feature Corpus Fixtures

These fixtures are abstract protocol-shape metadata for deterministic corpus and wire-feature checks.

They contain coarse phase, field, visibility, size, direction, and metadata-exposure descriptors only.
They do not contain packet captures, raw bytes, endpoint data, keys, nonces, proofs, or deployment data.

